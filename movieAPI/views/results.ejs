<h1>Results page</h1>

<ul>
    <% data["Search"].forEach(function(item) { %>
        <li>
            <strong>
                <%= item["Title"] %>
            </strong> 
            - <%= item["Year"] %>
        </li>
    <% }) %>
</ul>

<a href="/">Search Again!</a>